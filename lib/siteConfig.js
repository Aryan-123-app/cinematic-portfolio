// The public URL of the deployed portfolio site.
// Next.js automatically loads environment variables starting with NEXT_PUBLIC_
// on both the client and server sides.
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || 'https://vaibhav-create.vercel.app';
